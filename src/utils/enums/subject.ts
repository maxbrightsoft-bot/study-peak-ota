export enum TimerStatus {
    NotStarted,
    Started,
    Paused,
    Stopped
}

export enum SubjectType {
    Default,
    Korean,
    Math,
    English
}

export enum AlarmType {
    Default,
    Subject
}