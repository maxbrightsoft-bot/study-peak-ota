export enum NoteType {
    ToAcademy,
    ToStudent,
    AdminToAllAdmin,
    AdminToAllTeacher,
    TeacherToAllAdmin,
    TeacherToTeachers
}

export enum NoteSortColumn {
  Content = "Content",
  CreatedAt = "CreatedAt",
  Question = "Question"
}